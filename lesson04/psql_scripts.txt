insert into novel (title, description) values ('Точка зрения всеведущего читателя', 'История про тестировщика игр, который людит почитать новеллы'), ('Сказания о демонах и богах', 'Главный герой восстает против Демонического Бога'), ('Хризалида', 'Меравьиная колония в мире подземелий');
insert into author (name, bio, country) values ('Mad Snail', '', 'China'), ('RinoZ', 'Hi there, I am hoping you will enjoy my titles', 'Austrlia'), ('Sing-Shong', 'Hidden Schemer observes you', 'Korea');
insert into genre (name) values ('Боевые искусства'), ('Повседневность'), ('Фентези'), ('Подземелия'), ('ГГ умный'), ('Магия');

insert into genreext (genre_id, novel_id) values (1, 2), (3, 2), (5, 2), (3, 1), (5, 1), (6, 1), (3, 3), (4, 3), (5, 3), (6, 3);

insert into publisher (name, country) values ('Kakao Page', 'Korea');

create table novel (
id SERIAL PRIMARY KEY,
title varchar(128) NOT NULL,
description text
);

create table genre (
id SERIAL PRIMARY KEY,
name varchar(64)
);

create table genreext (
id SERIAL PRIMARY KEY,
genre_id integer REFERENCES genre,
novel_id integer REFERENCES novel
);

create table author (
id SERIAL PRIMARY KEY,
name varchar(128) NOT NULL,
bio text NOT NULL,
country varchar(64) NOT NULL
);

create table publisher (
id SERIAL PRIMARY KEY,
name varchar(128) NOT NULL,
country varchar(64) NOT NULL
);
